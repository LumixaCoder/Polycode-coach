# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
ENV/
env/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Build / Dist (PyInstaller)
build/
dist/
*.spec
*.zip
*.tar.gz
builds.log
build_info.json

# App progress — per-user, not repo
learning_progress.json
learning_progress.json.bak
*.bak

# DEV ONLY — kept locally, not on GitHub (user repo is lumixa.py + languages/ + data/ only)
learn_python_gui.py
generate_guided_hq_gif.py
build.py
dev.py
watch_build.py
tests/
_archive_modular_before_single_file/
app/
CODEMAP.md
NOTES.md
ARCHITECTURE_VISUAL.html
run_dev.bat
run_dev_windowed.bat
run_forever.bat
run_watch.bat
# PRIVATE helper docs — local only, never commit
FOR_YOU_ONLY_CODE_PROCESS.txt
PROJECT_SUMMARY.txt
*PRIVATE*
*private*
# Keep GitHub workflows for CI/Releases; ignore everything else in .github
.github/*
!.github/workflows
!.github/workflows/**
opencode.json

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Temp
*.tmp
*.log

# Data — keep data/ but ignore local installs
# (data/*.json are bundled — don't ignore)
